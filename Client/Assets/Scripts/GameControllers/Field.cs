using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class Field : MonoBehaviour
{
    public List<Cell> Cells;
    //public void CreateField(int x, int y, Vector2 _position)// Иницыализация поля
    //{
    //    Cells = new List<Cell>();
    //    for (int j = 0; j < y; j++)
    //    {
    //        for (int i = 0; i < x; i++)
    //        {

    //            Cells.Add(new Cell());
    //            Cells[Cells.Count - 1].InstantinateCell(new Vector3(_position.y + j * 20, 1, _position.x + i * 20), CellModel, i + j * x);
    //        }
    //    }
    //}
}

